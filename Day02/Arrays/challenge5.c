
#include <stdio.h>

int main() {
    int i,b,a,c;
    
    printf("le nombre element:");
        scanf("%d",&b);
        int T[b];
//min
        a>c;
    for(i=0;i<b;i++){
        printf("element %d:",i+1);
        scanf("%d",&T[i]);
        
        c=T[i];
        if(a>c){
        a=c;
        }
    }
    
   
    printf("min nombre est : %d",a);
    
    return 0;
}